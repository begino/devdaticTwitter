import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'devdacticTwitter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
